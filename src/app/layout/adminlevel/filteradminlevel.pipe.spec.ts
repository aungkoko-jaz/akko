import { FilteradminlevelPipe } from './filteradminlevel.pipe';

describe('FilteradminlevelPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteradminlevelPipe();
    expect(pipe).toBeTruthy();
    
  });
});
